name: Dependency Audit
description: Weekly check of pinned packages against latest PyPI versions with AI security summary
schedule: "0 9 * * 1"   # Monday 09:00 UTC
enabled: true
notify:
  provider: email
  "on": always
